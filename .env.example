# =================================
# ZITADEL INFRASTRUCTURE CONFIG
# =================================
# IMPORTANT: Copy this to .env and change all passwords!

# Master Key (MUST be exactly 32 characters)
ZITADEL_MASTERKEY=ChangeThisToYourSecure32CharKey!

# Database Passwords (CHANGE THESE!)
POSTGRES_PASSWORD=ChangePostgresPassword123!
ZITADEL_DB_PASSWORD=ChangeZitadelPassword456!

# Admin User Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeAdminPassword789!

# External Configuration (for local development)
ZITADEL_EXTERNAL_DOMAIN=localhost
ZITADEL_EXTERNAL_PORT=8080
ZITADEL_EXTERNAL_SECURE=false

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_DB=zitadel
ZITADEL_DB_USER=zitadel

# Project Configuration
COMPOSE_PROJECT_NAME=iot-platform